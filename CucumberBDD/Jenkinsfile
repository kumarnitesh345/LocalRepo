pipeline {

    agent any

    tools {
        maven 'Maven3'
        jdk 'JDK 17'
    }

    stages {

        stage('Checkout') {
            steps {
                git branch: 'master',
                    url: 'https://github.com/kumarnitesh345/LocalRepo.git'
            }
        }

        stage('Build & Test') {
            steps {
                dir('CucumberBDD') {
                    bat 'mvn clean test'
                }
            }
        }

        stage('Package') {
            steps {
                dir('CucumberBDD') {
                    bat 'mvn package -DskipTests'
                }
            }
        }

        /*stage('Docker Build') {
            steps {
                dir('CucumberBDD') {
                    bat 'docker build -t cucumber-framework .'
                }
            }
        }

        stage('Docker Run') {
            steps {
                dir('CucumberBDD') {
                    bat 'docker run --rm cucumber-framework'
                }
            }
        }*/
    }

    post {

       // always {
       //     junit allowEmptyResults: true,
       //           testResults: 'CucumberBDD2/target/junitreports/*.xml'

        //    archiveArtifacts artifacts: 'CucumberBDD/target/**/*.*',
        //                     fingerprint: true
       // }
        

        success {
            echo 'Build Successful'
        }

        failure {
            echo 'Build Failed'
        }
    }
}